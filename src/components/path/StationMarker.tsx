/** @deprecated Prefer LessonNode — kept so older imports keep type-checking. */
export { LessonNode as StationMarker } from "@/components/path/LessonNode";
export type { StationVisualState } from "@/components/path/types";
